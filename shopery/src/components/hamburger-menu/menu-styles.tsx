import styled from "styled-components";
import ThemeProps from "../../theme/theme-props.interface";

export const StyledMenu = styled.div<ThemeProps>`
  float: left;
  margin-top: 13px;
  cursor: pointer;
  font-size: 1.1em;
`;
